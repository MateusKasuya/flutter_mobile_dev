/// Breakpoints de layout responsivo centralizados.
///
/// Um "breakpoint" é a largura (em pixels lógicos) a partir da qual mudamos
/// o layout — aqui, o ponto em que passamos a tratar a tela como tablet.
/// Centralizar o valor evita repetir o literal 600 em várias telas e garante
/// que todas usem o mesmo limiar.
const double kTabletBreakpoint = 600;
