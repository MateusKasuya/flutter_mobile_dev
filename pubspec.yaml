name: frota_facil_mobile
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.2.0
  mask_text_input_formatter: ^2.9.0
  shared_preferences: ^2.5.4
  fluttertoast: ^9.0.0
  flutter_svg: ^2.2.3
  provider: ^6.1.5+1
  google_fonts: ^6.2.1
  image_picker: ^1.2.1
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/fonts/
